{
    "name": "api",
    "version": "1.0.0",
    "main": "app.js",
    "scripts": {
        "start": "node app.js",
        "test": "jest --coverage --coverageReporters=lcov",
        "start-and-test": "npm-run-all --parallel start test"
    },
    "dependencies": {
        "@sentry/node": "^7.57.0",
        "api": "file:",
        "express": "^4.18.2",
        "jest": "^29.7.0"
    }
}
